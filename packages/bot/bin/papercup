#!/usr/bin/env bash
# DEPRECATED — packages/bot/ is retiring. The canonical launcher is now
# packages/channels-plugin/dispatcher/bin/papercup. This stub forwards so
# existing muscle memory (and cron entries) keep working.
set -euo pipefail
exec "$(dirname "$0")/../../channels-plugin/dispatcher/bin/papercup" "$@"
