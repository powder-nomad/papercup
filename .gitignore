node_modules/
.env
.env.local
*.log
dist/
.DS_Store

# Per-package data + venv + downloaded models (regenerated by install)
packages/bot/data/
packages/bot/logs/
packages/voice-stack/sidecar/.venv/
packages/voice-stack/models/*.onnx
packages/voice-stack/models/*.bin

# Old root-level paths (pre-refactor) — keep ignored in case anything lingers
data/
logs/
sidecar/.venv/
models/*.onnx
models/*.bin

# VitePress build output
docs/.vitepress/dist/
docs/.vitepress/cache/

# Agent CLI runtime state created in the dispatcher cwd (agy/gemini/antigravity)
packages/channels-plugin/dispatcher/cache/
packages/channels-plugin/dispatcher/.antigravitycli/
